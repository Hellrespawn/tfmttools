{# This is a comment for jinja #}
{{- args[0] -}}
/
{{- albumartist or artist -}}
/
{%- if album -%}
    {%- if date -%}
      {{- date | year -}}
      {{- albumsort and "." ~ (albumsort | zero_pad(2)) -}}
      {{- " - " -}}
    {%- endif -%}
    {{- album -}}
    /
{%- endif -%}
{{- discnumber and discnumber | zero_pad(1) -}}
{{- tracknumber and tracknumber | zero_pad(2) ~ " - " -}}
{{- albumartist and artist ~ " - " -}}
{{- title -}}
